---
dependency:
  name: galaxy
  options:
    role-file: requirements.yml
lint:
  name: yamllint
driver:
  name: docker
platforms:
  - name: python_pip-alpine-latest
    image: alpine:latest
    command: sh -c "while true ; do sleep 1 ; done"
provisioner:
  name: ansible
  lint:
    name: ansible-lint
scenario:
  name: alpine-latest
